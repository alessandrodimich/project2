$('#microposts-start').prepend("<%= escape_javascript(render 'microposts/ajax_feed_item') %>");
$('#new_micropost')[0].reset();
$("#micropost-counter").html('<%= pluralize(current_user.microposts.count, "micropost") %>')